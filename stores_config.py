# -*- coding: utf-8 -*-
"""
店舗マスタ（stores_config.py）＝在庫融通アプリの「36店の固定リスト」（3法人）

【何をするスクリプトか】
店舗セルフアップロード型アプリの店名ドロップダウンに出す、36店の固定リストです。
アプリ本体（streamlit_app.py）と保管庫（gsheet_store.py）が共通で読み込みます。

★店名は「薬VANのファイル名の店名部分（店舗名_YYYYMM）」と、
  従来のコマンド版が使う店名（さと和光／氷川台 等）と同じ表記にそろえています。
  ＝ このドロップダウンで選んだ店名が、そのまま保管庫のキー（raw_<店名>）になります。
  ＝ 店ごとの合言葉（Secrets の [store_passwords]）のキーも、この店名そのままです。

★「和光」と「さと和光」は別会社（ソユーズ和光 と 内観堂さと和光）なので、必ず別項目です。

★2026-09-18 3法人対応（本間部長承認）：飛鳥薬局22店（ASUKA）を追加し、36店にしました。
  ・飛鳥22店の表記・並びは「事務マニュアル\\事務ポータル\\js\\api-client.js」の '飛鳥' 22件
    （＝事務ポータル飛鳥版の店名）と完全一致させています。合言葉を事務ポータルと同じ値で
    運用するため、店名のキーがずれると合言葉の突合ができなくなります。改名・接頭辞は付けません。
  ・画面の「○/N店」の N は len(STORE_NAMES) から自動計算＝36になります。
  ・法人での絞り込み（画面の①）は COMPANY_OF を使います。

【確定事項（本間部長 2026-07-25）】
  ・「厚生薬局」（ソユーズ店番号07）は閉店・対象外のため、この一覧から除外しました。
  ・各店の表記（例：東立石／柏駅前）が薬VANのファイル名と食い違う場合は、
    この一覧をその表記に直すだけです（分母は自動追従）。

※ venv不要。Windows専用パス。コメント・メッセージはすべて日本語です。
"""

# ソユーズ薬局（9店）… 店番号 01〜10 のうち 07 厚生薬局は閉店で除外
SOYOUZ = [
    '東大泉',    # 01
    '海浜幕張',  # 02
    '自由が丘',  # 03
    '東立石',    # 04
    '鈴木薬局',  # 05
    '大塚',      # 06
    # '厚生薬局',  # 07 ← 閉店・対象外のため除外（2026-07-25 本間部長確定）
    '和光',      # 08 ← ソユーズ和光（内観堂「さと和光」とは別会社）
    '柏',        # 09
    '柏駅前',    # 10
]

# 内観堂薬局（5店）… 店番号 31,32,33,34,35 の並び
NAIKANDO = [
    'みずほ台',  # 31
    'さと和光',  # 32 ← 内観堂さと和光（ソユーズ「和光」とは別会社）
    '氷川台',    # 33
    '徳丸',      # 34
    '下落合',    # 35
]

# 飛鳥薬局（22店）… 事務ポータル飛鳥版（api-client.js の '飛鳥' 22件）と同じ表記・同じ並び
#   ★2026-09-18 追加。店番号は事務ポータル側の並び（01〜22）に合わせている。
ASUKA = [
    '本店',        # 01
    '愛宕店',      # 02
    '元町薬局',    # 03
    '羽生店',      # 04
    '熊谷店',      # 05
    '浜町店',      # 06
    '新富町店',    # 07
    '新都心店',    # 08
    'あき薬局',    # 09
    'みやしろ',    # 10
    '富士見町店',  # 11
    '志多見店',    # 12
    '不動ヶ丘店',  # 13
    '江面店',      # 14
    '上新郷店',    # 15
    '北本店',      # 16
    'アリオ前店',  # 17
    '羽生南店',    # 18
    '春日部店',    # 19
    '羽生西口店',  # 20
    '久下店',      # 21
    '済生会通り店',  # 22
]

# ドロップダウンに出す36店（ソユーズ9→内観堂5→飛鳥22の順）
STORE_NAMES = SOYOUZ + NAIKANDO + ASUKA

# 法人名の一覧（画面の「法人で絞る」の選択肢の並び）
COMPANY_ORDER = ['ソユーズ', '内観堂', '飛鳥']

# 法人名 → その法人の店名リスト
STORES_BY_COMPANY = {
    'ソユーズ': list(SOYOUZ),
    '内観堂': list(NAIKANDO),
    '飛鳥': list(ASUKA),
}

# 会社名を引くための対応表（画面の見出し・絞り込み・引取依頼書の同一法人判定に使う）
COMPANY_OF = {}
for _n in SOYOUZ:
    COMPANY_OF[_n] = 'ソユーズ'
for _n in NAIKANDO:
    COMPANY_OF[_n] = '内観堂'
for _n in ASUKA:
    COMPANY_OF[_n] = '飛鳥'

# 店数（○/N の分母）
STORE_COUNT = len(STORE_NAMES)

# ★安全装置：店名の重複（同じ店名が2法人にある等）は保管庫のキー衝突になるので、読み込み時に止める。
if len(set(STORE_NAMES)) != len(STORE_NAMES):
    raise RuntimeError('stores_config.py：店名が重複しています。同じ店名を2つの法人に登録できません。')
